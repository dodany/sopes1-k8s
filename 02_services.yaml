apiVersion: v1
kind: Service
metadata:
  labels:
    app: load-client
  name: load-service
  namespace: project
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: load-client
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: load-client
    client: redis
  name: redis-client-service
  namespace: project
spec:
  ports:
  - port: 4000
    protocol: TCP
    targetPort: 4000
  selector:
    app: load-client
    client: redis
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: load-client
    client: go
  name: go-client-service
  namespace: project
spec:
  ports:
  - port: 3000
    protocol: TCPs
    targetPort: 3000
  selector:
    app: load-client
    client: go
